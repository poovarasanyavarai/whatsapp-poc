apiVersion: v1
kind: Service
metadata:
  name: whatsapp-service
  namespace: meta-whatsapp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  - port: 443
    targetPort: 8000
    protocol: TCP
    name: https
  selector:
    app: whatsapp-webhook